name: my-cloud-identity-facade
description: Facade stack that re-exports Pulumi stack outputs using namespaced keys.
backend:
  url: gs://spigell-infra-pulumi-states
runtime:
  name: nodejs
  options:
    packagemanager: yarn
    nodeargs: '--loader ts-node/esm --no-warnings'
