name: my-cloud-identity
description: My projects, accounts and keys for GCP, Cloudflare, etc...
backend:
  url: gs://spigell-infra-pulumi-states
runtime:
  name: nodejs
  options:
    packagemanager: yarn
    nodeargs: '--loader ts-node/esm --no-warnings'
